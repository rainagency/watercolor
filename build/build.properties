# Base Settings
HOME_DIR=${basedir}

# Flex resource locations
FLEX_HOME=/Applications/Flex SDKs/4.1.0
flexlib=${FLEX_HOME}
FLEX_TASKS=${FLEX_HOME}/ant/lib/flexTasks.jar
FLEX_UNIT=/Applications/Flex SDKs/flexunitframework
FLEX_CONFIG=${basedir}/build
PLAYER_PATH=${FLEX_HOME}/frameworks/libs/player/10.1/

# Lib Settings
PROJECT_NAME=yosemitewc
LIBRARY_NAME=yosemitewc
SRC_DIR=${HOME_DIR}/src
LIBS_DIR=${HOME_DIR}/libs
DEPLOY_DIR=${HOME_DIR}/bin-release

# Etc Settings
LOCALE=en_US
